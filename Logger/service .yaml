apiVersion: v1
kind: Service
metadata:
  name: shop-app-service
  labels:
    app: shop-app
spec:
  type: LoadBalancer
  selector:
    app: shop-app
  ports:
  - name: http
    port: 80
    targetPort: 8000
